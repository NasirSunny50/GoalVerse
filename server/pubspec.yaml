name: goalverse_server
description: GoalVerse competition backend (auth, predictions, scoring, leaderboards).
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.5.0

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  crypto: ^3.0.5
  http: ^1.2.2
  sqlite3: ^2.4.0

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
